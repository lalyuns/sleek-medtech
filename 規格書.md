# 輕量級醫材協作平台規格書

## 1. 目標

平台用來取代 LINE 傳遞重要醫材檔案，提供下列能力：

- STL 模型版本管理與版本溯源
- 材料參數、報告、回饋與成本的集中管理
- 專案權限控管與角色隔離
- 數位簽核、版本鎖定與稽核匯出
- 可審計的檔案上傳、hash、體積解析與 BOM 成本計算

## 2. 技術架構

| 層級 | 技術 | 功能 |
| --- | --- | --- |
| 後端 | Python, FastAPI | 提供 REST API、JWT 驗證、RBAC 權限檢查、上傳流程入口與核心業務邏輯。 |
| ORM / Migration | SQLAlchemy 2.0, Alembic | 將資料庫表格映射為 Python model，並管理 schema 版本遷移。 |
| 資料庫 | MySQL 8.0 | 儲存使用者、專案、模型版本、材料、回饋、報告、成本與稽核紀錄。 |
| 物件儲存 | MinIO，並保留 S3/GCS 相容設計 | 儲存 STL 與報告檔案；資料庫只保存檔案位置、hash 與業務 metadata。 |
| 任務佇列 | RQ + Redis | 將 STL 合併、hash 驗證、體積解析與檔案寫入改成背景工作，避免 API 請求長時間阻塞。 |
| 行程管理 | Supervisord | 在容器中管理 FastAPI、RQ worker 與清理任務，支援自動重啟。 |
| 認證 | JWT access token + refresh token | access token 用於呼叫 API；refresh token 用於續簽，登出時加入 Redis 黑名單。 |
| STL 處理 | numpy-stl | 解析 STL 幾何並計算體積，供 BOM 材料重量與成本計算使用。 |
| 前端 | React, Vite | 提供單頁式工作台、登入流程、專案管理與資料操作介面。 |
| 3D | Three.js, React Three Fiber | 載入 STL、顯示 3D 模型、支援剖面切割、視角控制與座標註記。 |
| 溯源圖 | React Flow | 將模型版本、前一版、回饋與報告呈現為可互動 traceability graph。 |
| 狀態 | Zustand | 管理登入狀態、3D viewer 狀態、pending pin 與註記狀態。 |

## 3. 資料庫模型

所有核心資料表支援軟刪除。刪除行為預設以軟刪除或停用取代實體刪除，查詢只回傳未刪除資料。

| 共用欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `is_deleted` | boolean | 軟刪除旗標。`false` 代表有效資料，`true` 代表資料已被刪除但仍保留於資料庫供稽核追溯。 |
| `deleted_at` | datetime, nullable | 軟刪除時間。未刪除時為空值；刪除後記錄刪除發生時間。 |

### 3.1 使用者、專案與權限

#### Users

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `user_id` | integer | 使用者唯一識別碼，供專案、上傳、回饋、簽核與稽核紀錄引用。 |
| `name` | string | 使用者顯示名稱，例如工程師姓名、醫師姓名或廠商窗口姓名。 |
| `email` | string, unique | 登入帳號，系統內不可重複。 |
| `hashed_password` | string | 加密後密碼雜湊，不儲存明文密碼。 |
| `role` | enum: `admin`, `engineer`, `doctor`, `vendor` | 全站角色。`admin` 可管理全站；`engineer` 負責上傳與成本資料；`doctor` 負責回饋與簽核；`vendor` 預設唯讀檢視。 |
| `created_at` | datetime | 帳號建立時間。 |

#### Projects

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `project_id` | integer | 專案唯一識別碼，作為版本、報告、成本與成員權限的主要關聯來源。 |
| `name` | string | 專案名稱，例如「下顎重建固定板 MR-2026-041」。 |
| `description` | text, nullable | 專案說明，描述臨床情境、製程需求或追蹤重點。 |
| `status` | enum: `active`, `archived` | 專案狀態。`active` 代表進行中，`archived` 代表封存不再主動操作。 |
| `owner_id` | FK -> `Users.user_id` | 專案建立者或主要負責人。 |
| `created_at` | datetime | 專案建立時間。 |

#### User Project Mappings

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `mapping_id` | integer | 專案成員權限紀錄的唯一識別碼。 |
| `user_id` | FK -> `Users.user_id` | 被加入專案的使用者。 |
| `project_id` | FK -> `Projects.project_id` | 使用者被授權存取的專案。 |
| `access_level` | enum: `read_only`, `edit`, `admin` | 專案層級權限。`read_only` 可檢視；`edit` 可上傳、回饋與新增成本/報告；`admin` 可管理成員與專案設定。 |

這組表如何運作：

`Users` 定義全站身份與角色，`Projects` 定義協作範圍，`User Project Mappings` 定義某個使用者在某個專案中的實際權限。後端 API 先驗證 JWT 得到使用者，再查詢 `User Project Mappings` 判斷是否能讀取、編輯或管理該專案。這可避免把醫師、工程師、廠商都放在同一個聊天室後無法控管權限的問題。

### 3.2 材料與模型版本

#### Materials

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `material_id` | integer | 材料唯一識別碼，供模型版本強制綁定。 |
| `name` | string | 材料名稱，例如 Titanium Ti-6Al-4V ELI、醫療不鏽鋼或 PEEK。 |
| `physical_parameters` | JSON | 材料物理與成本參數，至少包含密度、抗拉強度、單價與單價單位。 |
| `is_active` | boolean | 材料是否啟用。停用材料不可再被新模型版本選用，但歷史版本仍保留原材料關聯。 |

`physical_parameters` 至少包含：

| JSON 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `density` | number, g/cm3 | 材料密度，用於將 STL 體積換算為材料重量。 |
| `tensile_strength` | number | 抗拉強度或機械性質參數，用於材料規格管理與報告比對。 |
| `unit_price` | number | 材料單價，BOM 計算材料成本時使用。 |
| `unit_price_unit` | string, default `per_g` | 單價單位，目前預設每克計價。 |

`GET /materials/` 只回傳 `is_active = true` 且未刪除的材料。

#### Model Versions

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `version_id` | integer | 模型版本唯一識別碼。 |
| `project_id` | FK -> `Projects.project_id` | 此模型版本所屬專案。 |
| `uploader_id` | FK -> `Users.user_id` | 上傳此模型版本的使用者。 |
| `material_id` | FK -> `Materials.material_id` | 此版本使用的材料，為必填欄位。 |
| `file_url` | string | STL 檔案在 MinIO 或外部物件儲存中的位置；前端需透過授權 API 取得可讀 URL。 |
| `version_number` | integer | 專案內版本號，依專案自動遞增，例如 v1、v2。 |
| `description` | text, nullable | 版本說明，描述此版相對前版的幾何、製程或臨床回饋變更。 |
| `volume` | float, mm3 | worker 解析 STL 後得到的模型體積，以立方毫米儲存。 |
| `hash_value` | string | STL 檔案 SHA-256，用於防竄改與稽核比對。 |
| `status` | enum: `uploading`, `draft`, `locked` | 版本狀態。`uploading` 表示上傳或 worker 尚未完成；`draft` 可檢視與簽核；`locked` 表示已簽核鎖定。 |
| `total_chunks` | integer, nullable | 分塊上傳總數，用於驗證上傳完整性。 |
| `timestamp` | datetime | 模型版本建立時間。 |
| `signed_off_by` | FK -> `Users.user_id`, nullable | 簽核此版本的使用者。未簽核時為空。 |
| `signed_off_at` | datetime, nullable | 簽核完成時間。 |
| `signoff_reason` | text, nullable | 簽核理由，用於稽核說明。 |
| `signoff_user_snapshot` | JSON, nullable | 簽核當下的使用者快照，例如姓名、email、角色；避免日後使用者資料變更後失去當時簽核脈絡。 |

這組表如何運作：

`Materials` 提供材料參數，`Model Versions` 保存每次 STL 上傳後的版本紀錄。建立版本時必須綁定有效材料，且版本只能透過上傳流程產生，避免使用者手動建立沒有檔案、沒有 hash、沒有 STL 體積的假版本。BOM 會使用 `Model Versions.volume` 與 `Materials.physical_parameters` 計算材料重量與材料成本。

### 3.3 回饋、報告與溯源關聯

#### Feedbacks

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `feedback_id` | integer | 回饋唯一識別碼。 |
| `target_version_id` | FK -> `Model Versions.version_id` | 此回饋針對的模型版本。 |
| `author_id` | FK -> `Users.user_id` | 建立回饋的使用者，通常是醫師或管理員。 |
| `content` | text | 回饋內容，例如臨床修改建議、螺絲孔位置意見或製造注意事項。 |
| `coordinates` | JSON, nullable | 3D 座標 `{x, y, z}`。有座標時可在 3D viewer 顯示定位 pin。 |
| `status` | enum: `submitted`, `converted` | 回饋狀態。`submitted` 代表已提出；`converted` 代表已轉為需求或已被後續版本處理。 |
| `resolved_at` | datetime, nullable | 回饋被處理或轉需求的時間。 |

Feedback 支援新增、編輯、軟刪除。3D 座標註記會同步寫入資料庫。

#### Reports

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `report_id` | integer | 報告唯一識別碼。 |
| `project_id` | FK -> `Projects.project_id` | 報告所屬專案。 |
| `uploader_id` | FK -> `Users.user_id` | 上傳報告的使用者。 |
| `name` | string | 報告名稱，例如材料證明、ISO 10993 摘要或滅菌驗證。 |
| `report_type` | string | 報告分類，用於合規文件檢查與前端 filter。 |
| `file_url` | string | 報告檔案在物件儲存中的位置；前端需透過授權 API 取得可讀 URL。 |
| `created_at` | datetime | 報告上傳時間。 |

`report_type` 用於分類與合規缺件提示，例如：

- `material_test`
- `inspection`
- `regulatory`
- `manufacturing`
- `compliance`
- `sterilization`

刪除報告時，需同步移除指向該報告的溯源 reference edge。

#### Reference Edges

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `edge_id` | integer | 溯源關聯唯一識別碼。 |
| `source_version_id` | FK -> `Model Versions.version_id` | 關聯來源版本，也就是目前要追溯的模型版本。 |
| `target_type` | enum: `old_version`, `feedback`, `report` | 關聯目標類型。可連到前一版、醫師回饋或報告文件。 |
| `target_id` | integer | 關聯目標 ID。需搭配 `target_type` 判斷是版本 ID、回饋 ID 或報告 ID。 |

這組表如何運作：

`Feedbacks` 記錄醫師或協作成員對特定模型版本的意見，`Reports` 記錄支撐專案或版本的合規文件，`Reference Edges` 將模型版本、前一版、回饋與報告串成溯源圖。查詢 traceability 時，系統從某個 `Model Versions.version_id` 出發，透過 `Reference Edges` 找到版本 lineage、相關 feedback 與 report，回傳 React Flow 可視化資料。

### 3.4 成本與稽核

#### Costs

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `cost_id` | integer | 成本紀錄唯一識別碼。 |
| `project_id` | FK -> `Projects.project_id` | 成本所屬專案。 |
| `type` | enum: `labor`, `external_sample` | 成本類型。`labor` 代表工時或內部處理成本；`external_sample` 代表外部打樣、加工或測試費用。 |
| `amount` | decimal | 成本金額。 |
| `description` | text, nullable | 成本說明，例如 R&D contour adjustment 或 vendor titanium sample print。 |
| `created_at` | datetime | 成本紀錄建立時間。 |

#### Audit Logs

| 欄位 | 型別或值 | 解說 |
| --- | --- | --- |
| `log_id` | integer | 稽核紀錄唯一識別碼。 |
| `user_id` | FK -> `Users.user_id` | 執行操作的使用者。 |
| `action` | enum: `create`, `update`, `delete`, `upload`, `sign_off`, `change_material` | 操作類型，用於描述使用者做了什麼事。 |
| `entity_type` | enum: `model_version`, `material`, `feedback`, `report`, `user`, `cost`, `project` | 被操作的實體類型。 |
| `entity_id` | integer | 被操作實體的 ID，需搭配 `entity_type` 判讀。 |
| `timestamp` | datetime | 操作發生時間。 |
| `ip_address` | string, nullable | 操作者來源 IP，用於稽核與異常追蹤。 |
| `user_agent` | string, nullable | 操作者瀏覽器或用戶端資訊。 |
| `request_id` | string, nullable | 請求追蹤 ID，方便串接 log 與排查問題。 |
| `old_values` | JSON, nullable | 更新或刪除前的資料快照。 |
| `new_values` | JSON, nullable | 建立或更新後的資料快照。 |

這組表如何運作：

`Costs` 提供 BOM 中非材料成本，與模型版本的材料成本合併後形成總成本、報價與毛利率。`Audit Logs` 則橫跨所有核心表，記錄誰在何時對哪種實體做了什麼操作。稽核 API 需在回傳時補上可讀 context，例如操作者姓名、專案名、版本號與實體顯示名稱，讓稽核資料能回答「誰、何時、對哪個專案的哪個版本做了什麼」。

## 4. 權限模型

| 角色 | 權限 |
| --- | --- |
| 系統管理員 | 全站管理、材料、使用者、所有專案、稽核匯出 |
| 工程師 | 需加入專案且具 `edit` 或 `admin` 才能上傳模型、報告、成本 |
| 醫師 | 可檢視模型、建立 feedback、執行簽核 |
| 廠商 | 依專案授權檢視資料，預設唯讀 |

後端以 FastAPI dependency 檢查專案權限。前端也需依角色與 access level 隱藏不可用操作。

## 5. 認證

系統使用 JWT access token 與 refresh token。access token 用於呼叫需認證的 API，效期較短；refresh token 用於換發新的 token pair，效期較長。登出時 refresh token 需加入 Redis 黑名單，避免登出後仍可續簽。

### 登入

`POST /api/v1/auth/login`

| 項目 | 說明 |
| --- | --- |
| 輸入 | `email`、`password` |
| 驗證 | 後端以 `email` 查詢使用者，並用 bcrypt 驗證 `hashed_password`。 |
| 回傳 | `access_token`、`refresh_token`、使用者資訊。 |
| 前端行為 | 登入成功後保存 token，後續 API 以 `Authorization: Bearer <access_token>` 呼叫。 |

### Token 續簽

`POST /api/v1/auth/refresh`

refresh token 有效且未在 Redis 黑名單時，後端回傳新的 access token 與 refresh token。舊 refresh token 應失效，避免同一 token 被重複使用。

### 登出

`POST /api/v1/auth/logout`

登出時將 refresh token 加入 Redis 黑名單。前端需清除本機 token 與使用者狀態，並導回登入頁。

## 6. API 規格

所有需認證 API 使用：

`Authorization: Bearer <access_token>`

API 依用途分為三類：管理類 API、模型協作流程 API、查詢與稽核 API。

### 6.1 管理類 API

管理類 API 用於維護平台基礎資料、專案與成員權限。這些 API 通常需要 `admin` 或專案 `admin` 權限。

#### 材料

- `GET /api/v1/materials/`
- `POST /api/v1/materials/`
- `PUT /api/v1/materials/{material_id}`
- `DELETE /api/v1/materials/{material_id}`

維護可被模型版本綁定的材料規格。列表 API 只回傳啟用且未刪除材料；停用材料不可再被新版本選用。

#### 使用者

- `GET /api/v1/users/`
- `POST /api/v1/users/`
- `PUT /api/v1/users/{user_id}`
- `DELETE /api/v1/users/{user_id}`

管理平台帳號與全站角色。使用者角色決定全站能力，專案內實際權限仍需搭配 member mapping 判斷。

#### 專案與成員

- `GET /api/v1/projects/`
- `POST /api/v1/projects/`
- `GET /api/v1/projects/{project_id}`
- `GET /api/v1/projects/{project_id}/members`
- `POST /api/v1/projects/{project_id}/members`
- `DELETE /api/v1/projects/{project_id}/members/{user_id}`

建立專案、列出使用者可存取的專案、維護專案成員與專案層級權限。

### 6.2 模型協作流程 API

模型協作流程 API 是平台核心。模型版本不得由一般 API 手動建立，只能透過 STL 上傳流程產生，避免繞過檔案 hash、STL 體積解析、物件儲存與 worker 狀態追蹤。

#### 上傳

- `POST /api/v1/projects/{project_id}/versions/upload/init`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/upload/chunk`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/upload/complete`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/upload/status/{job_id}`

上傳流程：

1. 前端選擇材料、父版本、版本說明與 STL 檔案。
2. 前端計算檔案 SHA-256。
3. `upload/init` 建立 `uploading` 狀態的版本。
4. 前端分塊上傳至 MinIO 暫存區。
5. `upload/complete` 驗證分塊數量並 enqueue RQ job。
6. worker 合併分塊、驗證 SHA-256、解析 STL 體積、寫入 file_url、將版本改為 `draft`。
7. 前端輪詢 status，完成後刷新版本清單。

#### 版本

- `GET /api/v1/projects/{project_id}/versions`
- `GET /api/v1/projects/{project_id}/versions/{version_id}`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/lock`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/file-url`

查詢版本、取得受控檔案 URL、執行版本簽核。版本建立不在此 API 提供。

#### 回饋

- `GET /api/v1/projects/{project_id}/versions/{version_id}/feedbacks`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/feedbacks`
- `PUT /api/v1/projects/{project_id}/versions/{version_id}/feedbacks/{feedback_id}`
- `DELETE /api/v1/projects/{project_id}/versions/{version_id}/feedbacks/{feedback_id}`

讓醫師或授權使用者針對特定版本留下文字回饋與 3D 座標註記。回饋可編輯與軟刪除。

#### 報告

- `GET /api/v1/projects/{project_id}/reports`
- `POST /api/v1/projects/{project_id}/reports`
- `DELETE /api/v1/projects/{project_id}/reports/{report_id}`
- `GET /api/v1/projects/{project_id}/reports/{report_id}/file-url`

上傳、列出、下載與刪除材料、檢驗、合規等報告。刪除報告時需清理對應溯源關聯。

#### 溯源關聯

- `POST /api/v1/projects/{project_id}/versions/{version_id}/references`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/references`

允許 target：`old_version`、`feedback`、`report`。

建立版本與前一版、回饋、報告之間的溯源關係。一般使用者通常不需要直接理解此 API；多數情境由上傳新版本、建立 feedback、上傳 report 後透過 UI 建立或維護關聯。

### 6.3 查詢與稽核 API

查詢與稽核 API 用於彙整資料、產生成本與 traceability 證據。

#### BOM 成本

- `GET /api/v1/projects/{project_id}/bom`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/bom`
- `GET /api/v1/projects/{project_id}/costs`
- `POST /api/v1/projects/{project_id}/costs`

BOM 計算規則：

1. STL volume 以 mm3 儲存。
2. 計算材料重量前需轉換：`volume_mm3 / 1000 = volume_cm3`。
3. `volume_cm3 * density_g_per_cm3 = material_quantity_g`。
4. `material_quantity_g * unit_price = material_cost`。
5. `material_cost + labor + external_sample = total_cost`。

前端可輸入報價金額與幣別，計算毛利與毛利率。

#### 溯源圖

`GET /api/v1/projects/{project_id}/versions/{version_id}/traceability`

回傳 React Flow 結構：

- `nodes`
- `edges`

節點資料需包含：

- 版本：版本號、狀態、描述、hash、材料 ID、建立時間、簽核者快照、簽核時間、簽核理由
- 回饋：內容、狀態、作者、座標
- 報告：名稱、報告類型、建立時間、檔案 URL

前端 layout 規則：

- 模型版本固定在第一層，依版本號排序。
- feedback 固定在第二層。
- report 固定在第三層。
- previous version、feedback、report 使用不同線型與顏色。
- 右側 panel 顯示被選節點的稽核資訊。

#### 稽核

- `GET /api/v1/audit/logs`
- `GET /api/v1/audit/logs/export.csv`
- `GET /api/v1/audit/logs/export.pdf`
- `GET /api/v1/audit/export`

Audit 顯示格式需可讀：

「誰在何時對哪個專案的哪個版本或實體做了什麼」

## 7. 前端頁面

前端頁面以工作台為主，不做行銷式 landing page。每個頁面都應支援對應角色的主要工作流，且不顯示該角色不能執行的操作。

| 路由或區塊 | 使用者目標 | 主要內容 |
| --- | --- | --- |
| `/login` | 登入系統並取得 API token。 | Email、密碼、登入錯誤提示。 |
| `/projects` | 找到自己可操作的專案。 | 專案列表、建立專案、管理員入口。 |
| `/projects/{project_id}` | 日常協作工作台。 | 總覽、上傳、3D 檢視、BOM、報告、成員、稽核 tabs。 |
| `/projects/{project_id}/traceability` | 檢查版本脈絡與證據鏈。 | React Flow 溯源圖、節點資訊、版本切換。 |
| `/admin/materials` | 管理材料規格。 | 材料列表、新增、編輯、停用。 |
| `/admin/users` | 管理使用者。 | 使用者列表、新增、編輯、刪除。 |
| `/admin/audit` | 全站稽核檢視。 | 稽核列表、篩選、CSV/PDF 匯出。 |

### 專案工作台

專案工作台依角色與專案 access level 顯示 tabs：

- 總覽：呈現未簽核版本、待處理 feedback、缺少報告、BOM 狀態、待辦工作、最近活動與版本清單。
- 上傳：提供 STL 上傳 checklist，包含材料、父版本、版本說明、STL 檔案與 worker 狀態；按鈕 disabled 時需說明原因。
- 3D 檢視：載入 STL、自動置中、重設視角、旋轉/平移切換、XYZ 剖面切割、3D 定位 pin 與回饋編輯。
- BOM：顯示材料體積、重量、單價、工時、外部打樣費、報價金額、幣別、毛利與毛利率。
- 報告：上傳、分類、預覽、下載報告，並提示必備文件缺件。
- 成員：管理專案成員與專案權限。
- 稽核：顯示專案相關操作紀錄與 old/new values。

## 8. 版本鎖定與簽核

醫師或系統管理員可簽核 draft 版本。簽核代表該版本的 STL、材料、hash、報告與簽核紀錄已成為稽核證據，因此鎖定後不得覆寫或修改該版本內容。

簽核要求：

- 簽核理由
- 密碼二次確認
- 記錄簽核者 ID
- 記錄簽核時間
- 保存簽核者快照
- 寫入不可逆 audit log

版本鎖定後，後續變更必須上傳新 STL 版本，並透過 `Reference Edges` 建立新舊版本關係。這可確保歷史版本、臨床回饋、材料報告與簽核證據都能被追溯，而不是被覆蓋。

## 9. 稽核與合規原則

平台需能回答：

- 誰上傳了哪個模型？
- 此版本使用哪個材料？
- STL hash 是否與紀錄一致？
- 體積與 BOM 如何計算？
- 哪些 feedback 影響此版本？
- 哪些報告支持此版本？
- 誰簽核、何時簽核、簽核理由是什麼？
- 刪除或修改紀錄是否可追溯？

稽核輸出至少應包含：

- 操作者姓名、email 與角色
- 操作時間、IP、user agent 與 request id
- 專案名稱與版本號
- 操作類型與被操作實體
- old values 與 new values
- 相關報告、簽核紀錄與檔案 hash

## 10. 部署與執行

系統支援兩種執行模式：

| 模式 | 用途 | 組成 |
| --- | --- | --- |
| 本機開發模式 | 給工程師開發與除錯。 | Docker Compose 啟 MySQL、Redis、MinIO；後端、worker、前端在本機終端分別啟動。 |
| 完整容器模式 | 給展示或同學快速啟動。 | Docker Compose 啟 backend、frontend、MySQL、Redis、MinIO；backend 容器內由 Supervisord 管理 API、worker 與清理任務。 |

服務組成：

- backend FastAPI
- frontend Vite build / Nginx
- MySQL
- Redis
- MinIO
- RQ worker
- Supervisord

本地測試應至少通過：

- `alembic upgrade head`
- `python -m pytest -q`
- `npm run lint`
- `npm run build`
- `npm run test:e2e`

demo 測資可用 `backend/reset_demo_data.py` 重建。詳細啟動步驟、同學電腦建置方式與常見問題放在 `README.md`，本規格書只描述系統需要支援的執行模式。
