# 輕量級醫材協作平台規格書

## 1. 目標

平台用來取代 LINE 傳遞重要醫材檔案，提供下列能力：

- STL 模型版本管理與版本溯源
- 材料參數、報告、回饋與成本的集中管理
- 專案權限控管與角色隔離
- 數位簽核、版本鎖定與稽核匯出
- 可審計的檔案上傳、hash、體積解析與 BOM 成本計算

## 2. 技術架構

| 層級 | 技術 |
| --- | --- |
| 後端 | Python, FastAPI |
| ORM / Migration | SQLAlchemy 2.0, Alembic |
| 資料庫 | MySQL 8.0 |
| 物件儲存 | MinIO，並保留 S3/GCS 相容設計 |
| 任務佇列 | RQ + Redis |
| 行程管理 | Supervisord |
| 認證 | JWT access token + refresh token |
| STL 處理 | numpy-stl |
| 前端 | React, Vite |
| 3D | Three.js, React Three Fiber, drei |
| 溯源圖 | React Flow |
| 狀態 | Zustand |

## 3. 核心資料模型

所有核心資料表支援軟刪除：`is_deleted`、`deleted_at`。刪除行為預設以軟刪除或停用取代實體刪除，查詢只回傳未刪除資料。

### Users

- `user_id`
- `name`
- `email`
- `hashed_password`
- `role`: `admin`, `engineer`, `doctor`, `vendor`
- `created_at`

### Materials

- `material_id`
- `name`
- `physical_parameters`
- `is_active`

`physical_parameters` 使用 JSON，至少包含：

- `density`: g/cm3
- `tensile_strength`
- `unit_price`
- `unit_price_unit`: 預設 `per_g`

`GET /materials/` 只回傳 `is_active = true` 且未刪除的材料。

### Projects

- `project_id`
- `name`
- `description`
- `status`: `active`, `archived`
- `owner_id`
- `created_at`

### User Project Mappings

- `mapping_id`
- `user_id`
- `project_id`
- `access_level`: `read_only`, `edit`, `admin`

### Model Versions

- `version_id`
- `project_id`
- `uploader_id`
- `material_id`
- `file_url`
- `version_number`
- `description`
- `volume`: STL 解析出的 mm3
- `hash_value`: SHA-256
- `status`: `uploading`, `draft`, `locked`
- `total_chunks`
- `timestamp`
- `signed_off_by`
- `signed_off_at`
- `signoff_reason`
- `signoff_user_snapshot`

材料為強制綁定。沒有合法啟用材料，不可建立版本。

### Feedbacks

- `feedback_id`
- `target_version_id`
- `author_id`
- `content`
- `coordinates`: `{x, y, z}`
- `status`: `submitted`, `converted`
- `resolved_at`

Feedback 支援新增、編輯、軟刪除。3D 座標註記會同步寫入資料庫。

### Reports

- `report_id`
- `project_id`
- `uploader_id`
- `name`
- `report_type`
- `file_url`
- `created_at`

`report_type` 用於分類與合規缺件提示，例如：

- `material_test`
- `inspection`
- `regulatory`
- `manufacturing`
- `compliance`
- `sterilization`

刪除報告時，需同步移除指向該報告的溯源 reference edge。

### Reference Edges

- `edge_id`
- `source_version_id`
- `target_type`: `old_version`, `feedback`, `report`
- `target_id`

此表是版本溯源核心。模型版本、回饋與報告透過它組成可視化圖。

### Costs

- `cost_id`
- `project_id`
- `type`: `labor`, `external_sample`
- `amount`
- `description`
- `created_at`

### Audit Logs

- `log_id`
- `user_id`
- `action`: `create`, `update`, `delete`, `upload`, `sign_off`, `change_material`
- `entity_type`: `model_version`, `material`, `feedback`, `report`, `user`, `cost`, `project`
- `entity_id`
- `timestamp`
- `ip_address`
- `user_agent`
- `request_id`
- `old_values`
- `new_values`

稽核 API 需回傳可讀 context，例如操作者姓名、專案名、版本號、實體顯示名稱。

## 4. 權限模型

| 角色 | 權限 |
| --- | --- |
| 系統管理員 | 全站管理、材料、使用者、所有專案、稽核匯出 |
| 工程師 | 需加入專案且具 `edit` 或 `admin` 才能上傳模型、報告、成本 |
| 醫師 | 可檢視模型、建立 feedback、執行簽核 |
| 廠商 | 依專案授權檢視資料，預設唯讀 |

後端以 FastAPI dependency 檢查專案權限。前端也需依角色與 access level 隱藏不可用操作。

## 5. 認證

### Login

`POST /api/v1/auth/login`

輸入：

- `email`
- `password`

回傳：

- `access_token`
- `refresh_token`

### Refresh

`POST /api/v1/auth/refresh`

refresh token 有效時回傳新的 token pair。

### Logout

`POST /api/v1/auth/logout`

將 refresh token 加入 Redis 黑名單，阻止續簽。

## 6. API 規格

所有需認證 API 使用：

`Authorization: Bearer <access_token>`

### Materials

- `GET /api/v1/materials/`
- `POST /api/v1/materials/`
- `PUT /api/v1/materials/{material_id}`
- `DELETE /api/v1/materials/{material_id}`

### Users

- `GET /api/v1/users/`
- `POST /api/v1/users/`
- `PUT /api/v1/users/{user_id}`
- `DELETE /api/v1/users/{user_id}`

### Projects

- `GET /api/v1/projects/`
- `POST /api/v1/projects/`
- `GET /api/v1/projects/{project_id}`
- `GET /api/v1/projects/{project_id}/members`
- `POST /api/v1/projects/{project_id}/members`
- `DELETE /api/v1/projects/{project_id}/members/{user_id}`

### Versions

- `GET /api/v1/projects/{project_id}/versions`
- `GET /api/v1/projects/{project_id}/versions/{version_id}`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/lock`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/file-url`

模型版本只能由 STL 上傳流程建立。系統不得提供前端或一般 API 手動建立版本，避免繞過檔案雜湊、STL 體積解析、物件儲存與 worker 狀態追蹤。

### Upload

- `POST /api/v1/projects/{project_id}/versions/upload/init`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/upload/chunk`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/upload/complete`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/upload/status/{job_id}`

上傳流程：

1. 前端選擇材料、父版本、說明與 STL 檔案。
2. 前端計算檔案 SHA-256。
3. `upload/init` 建立 `uploading` 狀態的版本。
4. 前端分塊上傳至 MinIO 暫存區。
5. `upload/complete` 驗證分塊數量並 enqueue RQ job。
6. worker 合併分塊、解析 STL 體積、寫入 file_url、將版本改為 `draft`。
7. 前端輪詢 status，完成後刷新版本清單。

### Feedbacks

- `GET /api/v1/projects/{project_id}/versions/{version_id}/feedbacks`
- `POST /api/v1/projects/{project_id}/versions/{version_id}/feedbacks`
- `PUT /api/v1/projects/{project_id}/versions/{version_id}/feedbacks/{feedback_id}`
- `DELETE /api/v1/projects/{project_id}/versions/{version_id}/feedbacks/{feedback_id}`

### Reports

- `GET /api/v1/projects/{project_id}/reports`
- `POST /api/v1/projects/{project_id}/reports`
- `DELETE /api/v1/projects/{project_id}/reports/{report_id}`
- `GET /api/v1/projects/{project_id}/reports/{report_id}/file-url`

### Reference Edges

- `POST /api/v1/projects/{project_id}/versions/{version_id}/references`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/references`

允許 target：

- `old_version`
- `feedback`
- `report`

### BOM

- `GET /api/v1/projects/{project_id}/bom`
- `GET /api/v1/projects/{project_id}/versions/{version_id}/bom`
- `GET /api/v1/projects/{project_id}/costs`
- `POST /api/v1/projects/{project_id}/costs`

BOM 計算規則：

1. STL volume 以 mm3 儲存。
2. 計算材料重量前需轉換：`volume_mm3 / 1000 = volume_cm3`。
3. `volume_cm3 * density_g_per_cm3 = material_quantity_g`。
4. `material_quantity_g * unit_price = material_cost`。
5. `material_cost + labor + external_sample = total_cost`。

前端可輸入報價金額與幣別，計算毛利與毛利率。

### Traceability

`GET /api/v1/projects/{project_id}/versions/{version_id}/traceability`

回傳 React Flow 結構：

- `nodes`
- `edges`

節點資料需包含：

- 版本：版本號、狀態、描述、hash、材料 ID、建立時間、簽核者快照、簽核時間、簽核理由
- 回饋：內容、狀態、作者、座標
- 報告：名稱、報告類型、建立時間、檔案 URL

前端 layout 規則：

- 模型版本固定在第一層，依版本號排序。
- feedback 固定在第二層。
- report 固定在第三層。
- previous version、feedback、report 使用不同線型與顏色。
- 右側 panel 顯示被選節點的稽核資訊。

### Audit

- `GET /api/v1/audit/logs`
- `GET /api/v1/audit/logs/export.csv`
- `GET /api/v1/audit/logs/export.pdf`
- `GET /api/v1/audit/export`

Audit 顯示格式需可讀：

「誰在何時對哪個專案的哪個版本或實體做了什麼」

## 7. 前端頁面

### `/login`

登入頁。成功後進入 `/projects`。

### `/projects`

專案列表與管理入口。管理員可進入材料、使用者、全站稽核頁。

### `/projects/{project_id}`

專案工作台，依權限顯示 tabs：

- 總覽
- 上傳
- 3D 檢視
- BOM
- 報告
- 成員
- 稽核

總覽需顯示：

- 未簽核版本
- 待處理 feedback
- 缺少報告
- BOM 狀態
- 待辦工作
- 最近活動
- 版本清單

### 3D 檢視

功能：

- 載入 STL
- 自動置中與重設視角
- 旋轉與平移模式切換
- XYZ 剖面切割，slider range 依模型 bounding box 設定
- 點擊模型建立定位 pin
- 新增註解編輯器固定於右側回饋 panel，3D 畫面只保留定位 pin
- 既有註解可編輯與刪除

### 上傳

上傳頁需包含 checklist：

- 材料
- 父版本
- 版本說明
- STL 檔案
- worker 狀態

若按鈕 disabled，需顯示原因。頁面需明確說明模型版本必須透過 STL 上傳建立。

### Reports

功能：

- 上傳報告
- 報告類型分類
- 預設顯示最新報告
- 預覽與下載
- 必備報告缺件提示

### Members

功能：

- 左側新增成員
- 右側成員表格
- 顯示 email、角色、專案權限、最後活動時間
- 可調整專案權限或移除成員

### Audit

功能：

- 依 entity type 篩選
- 可讀敘事列表
- 顯示 old/new values
- CSV/PDF 匯出

## 8. 版本鎖定與簽核

醫師或系統管理員可簽核 draft 版本。

簽核要求：

- 簽核理由
- 密碼二次確認
- 記錄簽核者 ID
- 記錄簽核時間
- 保存簽核者快照
- 寫入不可逆 audit log

版本鎖定後不得修改該版本內容。後續變更需上傳新版本並建立版本溯源關係。

## 9. 稽核與合規原則

平台需能回答：

- 誰上傳了哪個模型？
- 此版本使用哪個材料？
- STL hash 是否與紀錄一致？
- 體積與 BOM 如何計算？
- 哪些 feedback 影響此版本？
- 哪些報告支持此版本？
- 誰簽核、何時簽核、簽核理由是什麼？
- 刪除或修改紀錄是否可追溯？

## 10. 部署與執行

服務組成：

- backend FastAPI
- frontend Vite build / Nginx
- MySQL
- Redis
- MinIO
- RQ worker
- Supervisord

本地測試應至少通過：

- `alembic upgrade head`
- `python -m pytest -q`
- `npm run lint`
- `npm run build`
- `npm run test:e2e`

demo 測資可用：

- `backend/reset_demo_data.py`

## 11. 規格邊界

本文件以目前實作路由、資料模型與 UI 行為為準。開發排程、分支策略、mock 分工與已淘汰端點不列入正式規格。
